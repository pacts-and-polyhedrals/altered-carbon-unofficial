{
  "name": "altered-carbon-foundry-monorepo",
  "private": true,
  "version": "1.0.0-rc1",
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "validate": "node scripts/validate-content.mjs && node scripts/validate-manifests.mjs",
    "build": "bash scripts/build.sh",
    "release:check": "npm test && npm run validate && npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
