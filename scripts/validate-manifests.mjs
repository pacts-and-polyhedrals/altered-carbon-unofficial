import fs from 'node:fs';for(const [file,id] of [['packages/altered-carbon-rpg/system.json','altered-carbon-rpg'],['packages/cold-storage/module.json','cold-storage']]){const m=JSON.parse(fs.readFileSync(file,'utf8'));if(m.id!==id)throw new Error(`${file} id mismatch`);if(String(m.compatibility.minimum)!=='14')throw new Error(`${file} must target Foundry 14`);console.log(`${file}: ${m.title} v${m.version}`)}
