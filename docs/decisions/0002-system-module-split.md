# ADR 0002 — Reusable system plus separate adventure module

The core rules implementation lives in `altered-carbon-rpg`; all Cold Storage-specific pregens, histories, identities, relationships, factions and mystery content live in `cold-storage`. This keeps the system reusable for other Altered Carbon games.
